# Make names without dots

Make names without dots

## Usage

``` r
make_names(nams)
```

## Arguments

- nams:

  (`character`)  
  vector of original names.

## Value

A `character` `vector` of proper names, which does not use dots in
contrast to [`make.names()`](https://rdrr.io/r/base/make.names.html).
