# Simulated CDISC data for examples

Simulated CDISC data for examples

## Usage

``` r
tern_ex_adsl

tern_ex_adae

tern_ex_adlb

tern_ex_adpp

tern_ex_adrs

tern_ex_adtte
```

## Format

`rds` (`data.frame`)

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 200
rows and 21 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 541
rows and 42 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
4200 rows and 50 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 522
rows and 25 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
1600 rows and 29 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
1000 rows and 28 columns.

## Functions

- `tern_ex_adsl`: ADSL data

- `tern_ex_adae`: ADAE data

- `tern_ex_adlb`: ADLB data

- `tern_ex_adpp`: ADPP data

- `tern_ex_adrs`: ADRS data

- `tern_ex_adtte`: ADTTE data
