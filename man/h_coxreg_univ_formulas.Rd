% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cox_regression.R
\name{h_coxreg_univ_formulas}
\alias{h_coxreg_univ_formulas}
\title{Helper for Cox Regression Formula}
\usage{
h_coxreg_univ_formulas(variables, interaction = FALSE)
}
\arguments{
\item{variables}{(named \code{list} of \code{strings})\cr list of additional analysis variables.}

\item{interaction}{(\code{flag})\cr if \code{TRUE}, the model includes the
interaction between the studied treatment and candidate covariate.}
}
\value{
It actually returns a vector of
character strings, coercible into formulas with
\code{\link[=as.formula]{as.formula()}}.
}
\description{
Creates a list of formulas. It is used internally in \code{\link[=h_coxreg]{h_coxreg()}}
for the comparison of univariate Cox regression models.
}
\examples{

h_coxreg_univ_formulas(
  variables = list(
    time = "time", event = "status", arm = "armcd", covariates = c("X", "y")
  )
)
h_coxreg_univ_formulas(
  variables = list(
    time = "time", event = "status", arm = "armcd", covariates = c("X", "y"),
    strata = "SITE"
  )
)
h_coxreg_univ_formulas(
  variables = list(
    time = "time", event = "status", arm = "armcd", covariates = c("X", "y"),
    strata = "SITE"
  ),
  interaction = TRUE
)

}
