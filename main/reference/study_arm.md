# Indicate study arm variable in formula

We use `study_arm` to indicate the study arm variable in `tern`
formulas.

## Usage

``` r
study_arm(x)
```

## Arguments

- x:

  arm information

## Value

`x`
