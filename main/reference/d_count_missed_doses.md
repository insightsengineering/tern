# Description function that calculates labels for `s_count_missed_doses()`

**\[stable\]**

## Usage

``` r
d_count_missed_doses(thresholds)
```

## Arguments

- thresholds:

  (`numeric`)  
  minimum number of missed doses the patients had.

## Value

`d_count_missed_doses()` returns a named `character` vector with the
labels.

## See also

[`s_count_missed_doses()`](https://insightsengineering.github.io/tern/reference/count_missed_doses.md)
